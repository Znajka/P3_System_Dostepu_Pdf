The 20-Step Prompt Roadmap
####################################
(done by RJ)Phase 1: Foundation & Database
(done by RJ)Prompt: "Act as a Lead Architect. Define a docker-compose.yml for a Spring Boot (Java 21), FastAPI (Python 3.12), and PostgreSQL environment."

(done by RJ)Prompt: "Generate the SQL schema for tables: USER, DOCUMENT, DOCUMENT_KEY_METADATA, ACCESS_GRANT, ACCESS_EVENT_LOG, and SESSION_TOKEN based on the provided requirements."

(done by RJ)Prompt: "Create the Spring Boot JPA Entities for these tables, ensuring correct relationships (e.g., One-to-Many for User to AccessGrants)."




####################################
Phase 2: Spring Boot Auth & Core
(done by RJ)Prompt: "Configure Spring Security with JWT-based authentication and Bcrypt password hashing. Include roles: ADMIN, USER, OWNER."

(done by RJ)Prompt: "Implement POST /documents in Spring Boot to handle file metadata storage. The actual file should be forwarded to the FastAPI service for encryption."

(done by RJ)Prompt: "Create the service logic for POST /documents/{id}/grant. Only the OWNER or ADMIN can grant access. Include an expiration_time field."

(done by RJ)Prompt: "Implement GET /documents/{id}/open-ticket. This should check if a valid ACCESS_GRANT exists and return a signed, 60-second JWT ticket."

(done by RJ)Prompt: "Implement the ACCESS_EVENT_LOG service to record every attempt to grant, revoke, or open a document."




####################################
Phase 3: FastAPI Security & Crypto
(done by RJ)Prompt: "In FastAPI, create a utility using the cryptography library to encrypt a PDF file using AES-256 with a key provided via environment variables."

(done by RJ)Prompt: "Create a FastAPI endpoint POST /internal/encrypt that receives a file from Spring Boot, encrypts it, and saves it to a secure local volume."

(done by RJ)Prompt: "Implement a FastAPI dependency to validate the 'Open Ticket' JWT generated by Spring Boot."

(done by RJ)Prompt: "Create a FastAPI streaming endpoint GET /stream/{ticket}. It must decrypt the PDF on-the-fly and stream it as application/pdf without writing the decrypted file to disk."




####################################
Phase 4: Frontend (PDF.js)
Prompt: "Set up a React/Vue frontend with PDF.js. Create a component that fetches a PDF as an ArrayBuffer using the 'Open Ticket' from Spring Boot."

Prompt: "Configure the PDF.js viewer to render the PDF onto a <canvas> element. Ensure the 'Download' and 'Print' buttons are disabled/hidden."

Prompt: "Implement a 'Secure Viewer' wrapper that prevents right-clicks and basic 'Save As' shortcuts on the canvas page."



####################################
Phase 5: Advanced Security & Cleanup
Prompt: "In Spring Boot, implement a RateLimiter using a library like Bucket4j to lock users after 5 failed access attempts."

Prompt: "Create a scheduled task in Spring Boot to automatically revoke ACCESS_GRANT records where expiration_time has passed."

Prompt: "Refactor the 'Open Ticket' logic to include the user's IP address in the JWT payload for IP-pinning security."

Prompt: "Write an integration test suite where an ADMIN grants access to a USER, and we verify the USER can fetch a ticket while a non-authorized user cannot."

Prompt: "Final Review: Scan all files for hardcoded secrets or insecure headers. Generate a README.md explaining the key rotation process."